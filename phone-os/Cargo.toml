[package]
name = "phone_os"
version = "0.1.0"
edition = "2024"

[dependencies]
toml = { version = "1.1.4", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
